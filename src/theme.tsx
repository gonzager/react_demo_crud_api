import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    primary: {
      main: "#5aa123",
    },
    secondary: {
      main: "#4db6ad",
    },
  },
});

export default theme;
